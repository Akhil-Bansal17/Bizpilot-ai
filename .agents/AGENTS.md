# Project Rules & Customizations

## GitHub Configuration & Deployment Rules

This project must strictly use the following GitHub repository settings:

- **GitHub Username:** `Akhil-Bansal17`
- **Repository:** `Bizpilot-ai`
- **Exact GitHub URL:** `https://github.com/Akhil-Bansal17/Bizpilot-ai.git`
- **Target Branch:** `main`

### Execution Directives:
1. This is the **ONLY** GitHub repository to use for this project.
2. NEVER guess, substitute, or switch to another repository.
3. NEVER push this project to any similarly named repository.
4. NEVER create a new repository for this project.
5. NEVER change the GitHub remote to another repository unless explicitly instructed.
6. NEVER force-push, delete branches, reset history, or rewrite Git history unless explicitly instructed.
7. Before every push, verify:
   - Operating inside the correct local project folder (`c:\Users\hp\OneDrive\Desktop\bizpilot-ai`).
   - Current Git branch is `main`.
   - Configured `origin` remote points exactly to `https://github.com/Akhil-Bansal17/Bizpilot-ai.git`.
   - Repository status and pending changes.
8. If ANYTHING does not match, STOP and ask the user instead of guessing.
